# jhovahn-gibbs-portfolio

Personal developer site for Jhovahn Gibbs. It's a single static page (`index.html`) with no build step.

Open `index.html` in a browser to view it locally. To deploy, drop the folder onto any static host (GitHub Pages, Vercel, Netlify).
